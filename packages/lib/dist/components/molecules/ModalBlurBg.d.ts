import React, { ReactNode } from 'react';
import { ModalProps } from 'react-native-modal';
export declare const ModalBlurBg: ({
  children,
  ...props
}: {
  children: ReactNode;
} & Partial<ModalProps>) => React.JSX.Element;
