import React from 'react';
type Props = {
  children: React.ReactNode | React.ReactNode[];
  expandableContent: React.ReactNode | React.ReactNode[];
};
export declare const ExpandingCard: ({
  children,
  expandableContent,
}: Props) => React.JSX.Element;
export {};
