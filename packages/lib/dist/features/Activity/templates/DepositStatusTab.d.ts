import React from 'react';
import { ActivityProductHook, ActivityTabProps } from '../types';
export declare const DepositStatusTab: ({
  depth,
  productId,
  onActivityPress,
  ...listProps
}: ActivityTabProps & ActivityProductHook) => React.JSX.Element;
