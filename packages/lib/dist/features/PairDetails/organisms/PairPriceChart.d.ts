import React from 'react';
interface PriceChartProps {
  instrumentId: number;
  skipQuery?: boolean;
}
export declare const PairPriceChart: ({
  instrumentId,
  skipQuery,
}: PriceChartProps) => React.JSX.Element;
export {};
