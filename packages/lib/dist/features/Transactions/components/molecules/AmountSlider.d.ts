import React from 'react';
type Props = {
  maxQuantity: number;
  decimalPlaces: number;
};
export declare const AmountSlider: ({
  maxQuantity,
  decimalPlaces,
}: Props) => React.JSX.Element;
export {};
