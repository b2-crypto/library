import React from 'react';
type Props = {
  children: React.ReactNode;
  onClose: () => void;
};
export declare const OrderFormHeader: ({
  children,
  onClose,
}: Props) => React.JSX.Element;
export {};
