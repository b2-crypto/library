import React from 'react';
type Props = {
  /** Array of order types (machine-readable strings) */
  types?: Readonly<string[]>;
};
export declare const OrderTypeField: ({ types }: Props) => React.JSX.Element;
export {};
