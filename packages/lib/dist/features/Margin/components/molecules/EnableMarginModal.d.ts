import * as React from 'react';
export declare function EnableMarginModal({
  isEnabled,
  onEnable,
  loading,
  error,
}: {
  isEnabled: boolean;
  onEnable: () => void;
  loading: boolean;
  error?: string;
}): React.JSX.Element;
